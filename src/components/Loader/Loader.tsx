import "./index.css";

const Loader = () => {
    return (
        <div data-testid={"loader"} className={"loader-container"}>
            <div className={"loader"} />
        </div>
    );
};

export { Loader };
